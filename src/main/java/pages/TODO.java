package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

import java.util.List;

public class TODO {

/*

    public void selectExample(String exampleLinkText) {
        WebElement example = driver.findElement(By.linkText(exampleLinkText));
        example.click();
    }

    public void selectSubLinkExample(String sublinkText) {
        WebElement subLinkexample = driver.findElement(By.linkText(sublinkText));
        subLinkexample.click();
    }

    public void selectExampleSubLink(String exampleLinkText, String sublinkText) {
        WebElement example = driver.findElement(By.linkText(exampleLinkText));
        example.click();
        WebElement subLinkexample = driver.findElement(By.linkText(sublinkText));
        subLinkexample.click();
    }

    public void getElementsByTagName(String tagName) {
        List<WebElement> linkref = driver.findElements(By.tagName(tagName));
        System.out.println("The Number of links with " + tagName + "are :" + linkref.size());
        for (int i = 0; i < linkref.size(); i++) {
            System.out.println(linkref.get(i).getText());
            //linkref.get(i).click();
            //driver.navigate().back();
        }
    }

    public void getElementsByCss(String cssSelector) {
        List<WebElement> linkref = driver.findElements(By.tagName(cssSelector));
        System.out.println("The Number of links with " + cssSelector + "are :" + linkref.size());
        for (int i = 0; i < linkref.size(); i++) {
            System.out.println(linkref.get(i).getText());
            //linkref.get(i).click();
            //driver.navigate().back();
        }
    }
*/

}
